# --- Required ---
# GitHub token for CIAnalyzer to access the API
GITHUB_TOKEN=ghp_your_token_here

# Comma-separated list of repositories to analyze
TARGET_REPOSITORIES=owner/repo1,owner/repo2

# --- Optional ---
# Configuration for the Flask web server
FLASK_DEBUG=True
FLASK_PORT=5001

# Logging configuration
LOG_LEVEL=INFO

# CIAnalyzer Docker image (use hello-world for testing)
CIANALYZER_IMAGE=kesin11/cianalyzer:v6

# Token security level (auto, file, stdin, secret, env)
# auto: Try all methods in order of security
# file: Use secure temporary file mount (most secure)
# stdin: Pass token via stdin (secure)
# secret: Use Docker secrets (requires Docker Swarm)
# env: Use environment variable (less secure)
TOKEN_SECURITY_LEVEL=auto
